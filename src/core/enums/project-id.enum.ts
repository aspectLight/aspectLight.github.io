/** Stable anchors for projects: the element id of each entry, linked from the Currently block. */
export enum ProjectId {
  PolygonReconstruction = 'polygon-reconstruction',
  IsingMachine = 'ising-machine',
  Lyra = 'lyra',
}

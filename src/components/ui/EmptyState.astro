---
import { revealDelay } from '@core/motion/reveal-delay';

interface Props {
  readonly message: string;
}

const { message } = Astro.props;
---

<p class="empty-state" data-reveal style={revealDelay(1)}>
  {message}
</p>

<style>
  .empty-state {
    color: var(--color-muted);
  }
</style>

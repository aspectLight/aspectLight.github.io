---
interface Props {
  readonly id: string;
  readonly number: number;
  readonly title: string;
}

const { id, number, title } = Astro.props;
const headingId = `${id}-heading`;
const marker = `§ ${String(number)}`;
---

<section class="section" id={id} aria-labelledby={headingId} data-marker={marker} data-reveal>
  <h2 class="section__heading" id={headingId}>
    {marker} — {title}
  </h2>
  <slot />
</section>

<style>
  .section {
    border-block-start: var(--line-width) solid var(--color-rule);
    padding-block: var(--space-section);
    padding-inline: var(--layout-offset) var(--space-gutter);
  }

  .section__heading {
    color: var(--color-accent);
    font-size: var(--type-label-size);
    font-weight: 400;
    letter-spacing: var(--type-label-tracking);
    line-height: var(--type-label-leading);
    margin-block-end: var(--space-6);
  }
</style>

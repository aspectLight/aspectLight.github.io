<!-- A faint decorative figure in the top-right corner of its section, below the heading rule. -->
<div class="watermark" aria-hidden="true">
  <slot />
</div>

<style>
  .watermark {
    color: var(--color-ink);
    display: none;
    inset-block-start: calc(var(--space-section) + var(--space-8));
    inset-inline-end: var(--space-gutter);
    opacity: var(--watermark-opacity);
    pointer-events: none;
    position: absolute;
    user-select: none;
  }

  @media (width >= 1100px) {
    .watermark {
      display: block;
    }
  }
</style>
